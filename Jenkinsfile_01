#!groovy

pipeline {
    agent any
    
       stages {
        stage("Build") {
            steps {
     def MVHome = tool name: 'maven-1', type: 'maven'
                sh "${MVHome}/bin/mvn -version"
                sh "${MVHome}/bin/mvn clean install"
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}
